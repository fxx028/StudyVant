<view>
<view bindtap="clickSearch">
<van-search
  placeholder="请输入搜索关键词"
  disabled="true"
  shape="round"
>
</van-search>
</view>


<van-grid column-num="3">
  <van-grid-item bind:click="onClickBook" icon="{{baseUrl+item.cover}}" text="{{item.title}}" wx:for="{{ recommend.books }}"/>
</van-grid>

</view>

